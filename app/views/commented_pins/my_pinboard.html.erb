 <div class="transitions-enabled grid" id="pins" data-packery='{ "img ": ".grid-item" }'>
<% @pins.each do |pin| %>
    <div class="box panel panel-default grid-item">
      <h2><%=  link_to pin.title, pin %></h2>
      <%=  link_to (image_tag pin.image.url), pin %>
    </div>
  <% end %>
</div>
